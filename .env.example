# 로컬 개발 기본값: Vite 프록시(/api -> localhost:8787) 사용
VITE_API_URL="/api"

# 필요 시 로컬 프록시 대상 Worker 주소 변경
# VITE_LOCAL_WORKER_URL="http://127.0.0.1:8787"

# 운영 배포 시에는 실제 Worker URL 사용
# VITE_API_URL="https://your-worker.your-subdomain.workers.dev"
